# Website

This portfolio site is built with React and Vite.

## Installation

```bash
npm install
```

## Local Development

```bash
npm start
```

This starts the Vite development server (usually at http://localhost:5173).

## Build

```bash
npm run build
```

Static output is written to the `dist` directory. Preview it with:

```bash
npm run preview
```
